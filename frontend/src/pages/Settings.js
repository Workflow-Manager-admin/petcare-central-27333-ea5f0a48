import React from "react";

// PUBLIC_INTERFACE
function Settings() {
  return (
    <div>
      <h2>Settings</h2>
      <div style={{marginTop:"28px", color:"var(--color-muted)"}}>
        Settings options will be available here in the future.
      </div>
    </div>
  );
}

export default Settings;
